const onlinePatientStyles = (theme) => ({
  paper: {
    marginTop: theme.spacing(2),
    // padding: theme.spacing(2),
  },
  field: {
    marginTop: theme.spacing(1),
    marginBottom: theme.spacing(1),
  },
  button: {
    marginRight: theme.spacing(2),
  },
  containerTable: {
    maxHeight: 440,
  },
});
export default onlinePatientStyles;
