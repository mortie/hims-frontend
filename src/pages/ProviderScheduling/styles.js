const providerSchedulingStyles = (theme) => ({
  toolbarRoot: {
    position: "relative",
  },
  progress: {
    position: "absolute",
    width: "100%",
    bottom: 0,
    left: 0,
  },
  paper: {
    marginBottom: theme.spacing(1),
    padding: theme.spacing(1)
  },
});

export default providerSchedulingStyles;
