const userAvatarStyles = (theme) => ({
  root: {
    gap: "6px",
    alignItems: "center",
  },
  flexRow: {
    display: "flex",
    flexDirection: "row",
  },
  flexColumn: {
    display: "flex",
    flexDirection: "column",
  },
});

export default userAvatarStyles;
